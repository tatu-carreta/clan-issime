{% macro input(articulo, arrayColores, talles, paths) %}
 <script type="text/javascript">
	 tipoUsuario = 2;
 </script>

            <p><span class="numeroArt">Art: {{articulo.codigoArticulo}}</span> / <span class="nombreArtículo"><strong>{{articulo.nombreArticulo}}</strong></span> / <strong>Material:  {{articulo.material}}</strong></p>
            <p class="precio">$ {{articulo.precioMayorista}}
		Precio mayorista</p>
		<h4>Seleccioná color, talle y cantidad</h4>
		{% for cor in arrayColores%}
				
		<div class="contentSeleccionArt">
			<div class="contentArt">
				<img class="miniatura actualizarCarrousel" id="{{cor.color.idColor}}" src="{{paths.PATH_IMAGES_CATALOGO}}{{cor.imgChicas[0].nombreImagen}}" alt="Clan-Issime">
				<div class="contentCheckTallesH">
				{% for tall in cor.talles%}
			
					{% if tall.valor == 1 %}
						<div class="comboTalleCantidad">
							<label for="{{tall.nombreTalle}}" class="labelTalle agregarMayorista">{{tall.nombreTalle}}
							<input class="checkTalle" type="hidden" value="{{tall.idTalle}}" name="talle24" ></label>
							<input class="textoTalle" type="text" name="talle24" id="{{tall.nombreTalle}}" value="0">
						</div>
					{% endif %}
							
				{% endfor%}
				</div>
			</div>

		</div><!--cierra contentSeleccionArt-->		
		{%endfor%}
		
			 
                <a href="#tuCompra" class="btnGris" id="agregarAlCarrito">Agregar al carrito</a>  
		
{% endmacro %}